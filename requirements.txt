# Core pipeline — everything below is required.
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
statsmodels>=0.14
openpyxl>=3.1
python-docx>=1.1
matplotlib>=3.7

# Optional. The LSTM, GRU and 1D-CNN run on NumPy implementations contained in
# earthquake_analysis.py when neither of these is importable, so the pipeline
# never depends on them. Install one only to train the networks on a
# GPU-capable framework.
# tensorflow>=2.15
# torch>=2.2
